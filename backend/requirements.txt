fastapi>=0.115.0,<1
uvicorn>=0.30.0,<1
httpx>=0.27.0,<1
python-dotenv>=1.0.0,<2
pydantic>=2.0.0,<3
# The backend spawns the MCP server with its own interpreter, so it needs the
# MCP server's dependencies too (mcp client SDK, openai, feedparser, slugify)
-r ../mcp_server/requirements.txt
